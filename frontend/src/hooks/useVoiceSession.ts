/** Hook for managing voice interview session state and recording. */

export function useVoiceSession() {
  // Stub — implementing Sprint 2
  return { recording: false, start: () => {}, stop: () => {} };
}
