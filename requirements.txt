# Production dependencies — only what's actually imported at runtime
# Heavy unused packages (chromadb, spacy, celery, langgraph, openai) excluded
# to keep Render free-tier build under the 512 MB RAM limit.

# Web framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
python-multipart>=0.0.9

# Settings & validation
pydantic>=2.9.0
pydantic-settings>=2.5.0

# Resume parsing
pdfplumber>=0.11.0
python-docx>=1.1.0

# AI — Claude
anthropic>=0.39.0

# Database — Supabase
supabase>=2.9.0

# HTTP client (scrapers, outreach)
httpx>=0.27.0
